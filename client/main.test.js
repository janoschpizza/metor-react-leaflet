import { assert } from 'chai'

describe('Test #1', function () {
  it('is true', function () {
    assert.isTrue(true)
  })
  it('is false', function () {
    assert.isFalse(false)
  })
})

// describe('<App/>', function () {
//   it('is instance of Component', function () {
//     assert.instanceOf(App, Component)
//   })
// })
